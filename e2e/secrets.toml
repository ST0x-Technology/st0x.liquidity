[evm]
rpc_url = "http://localhost:9999"

# Parse-only template: no server ever boots from this file. The e2e harness
# builds its broker context programmatically with the Alpaca client in mock
# mode against an in-process mock server (see tests/e2e/hedging/assertions.rs
# and tests/e2e/full_system.rs). `mode` is omitted here because the mock mode
# variant only compiles under test features; a parse of this template must
# succeed under every feature set.
[broker]
type = "alpaca-broker-api"
api_key = "test-key"
api_secret = "test-secret"
account_id = "904837e3-3b76-47ec-b432-046db621571b"

[issuance]
base_url = "http://localhost:8000"
api_key = "0xaabbccddeeff00112233445566778899aabbccddeeff00112233445566778899"

[pricing]
api_key = "e2e-pricing-key"
