server_port = 8080
log_level = "debug"
database_url = ":memory:"
hyperdx.service_name = "st0x-hedge"

[raindex]
orderbook = "0x1111111111111111111111111111111111111111"
deployment_block = 1

[rebalancing]
redemption_wallet = "0xbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb"
equity = { target = 0.5, deviation = 0.2 }
usdc = { target = 0.5, deviation = 0.3 }

# Wallet configuration — the wallet provider is selected at compile
# time via cargo features (wallet-turnkey or wallet-private-key).
# Include all fields; only those matching the compiled feature are
# read, the rest are silently ignored.
[rebalancing.wallet]
address = "0xaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"
# Required for wallet-turnkey:
organization_id = "org-example-turnkey-org-id"

[assets.equities.EXAMPLE]
trading = "enabled"
rebalancing = "disabled"
tokenized_equity = "0xaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"
tokenized_equity_derivative = "0xbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb"
