# RPC provider must support debug_traceTransaction for Pyth price extraction
# Format: wss://lb.drpc.org/base/API_KEY or https://lb.drpc.org/base/API_KEY
# Recommended: dRPC (https://drpc.org) - Free tier: 210M compute units/month
# Example: WS_RPC_URL=wss://lb.drpc.org/base/YOUR_API_KEY_HERE
WS_RPC_URL=${WS_RPC_URL}
ORDERBOOK=${ORDERBOOK}
ORDER_OWNER=${ORDER_OWNER}
DEPLOYMENT_BLOCK=${DEPLOYMENT_BLOCK}

# Executor selection: schwab, alpaca-trading-api, alpaca-broker-api, dry-run
EXECUTOR=${EXECUTOR}

# Schwab credentials (required when EXECUTOR=schwab)
SCHWAB_APP_KEY=${SCHWAB_APP_KEY}
SCHWAB_APP_SECRET=${SCHWAB_APP_SECRET}
ENCRYPTION_KEY=${ENCRYPTION_KEY}

# Alpaca Trading API credentials (required when EXECUTOR=alpaca-trading-api)
ALPACA_API_KEY=${ALPACA_API_KEY}
ALPACA_API_SECRET=${ALPACA_API_SECRET}

# Alpaca Broker API credentials (required when EXECUTOR=alpaca-broker-api)
ALPACA_BROKER_API_KEY=${ALPACA_BROKER_API_KEY}
ALPACA_BROKER_API_SECRET=${ALPACA_BROKER_API_SECRET}
ALPACA_ACCOUNT_ID=${ALPACA_ACCOUNT_ID}
# Alpaca Broker API mode: sandbox or production
ALPACA_BROKER_API_MODE=${ALPACA_BROKER_API_MODE}

# Rebalancing configuration (required when REBALANCING_ENABLED=true)
REBALANCING_ENABLED=${REBALANCING_ENABLED}
REDEMPTION_WALLET=${REDEMPTION_WALLET}
ETHEREUM_RPC_URL=${ETHEREUM_RPC_URL}
EVM_PRIVATE_KEY=${EVM_PRIVATE_KEY}
USDC_VAULT_ID=${USDC_VAULT_ID}
# Base chain RPC URL (for vault operations)
BASE_RPC_URL=${BASE_RPC_URL}
BASE_ORDERBOOK=${BASE_ORDERBOOK}

# Optional: HyperDX observability integration
# Enables trace export to HyperDX for real-time monitoring and debugging
# If not set, the bot runs normally with console-only logging
# Get your API key from https://www.hyperdx.io/
HYPERDX_API_KEY=${HYPERDX_API_KEY}

RUST_LOG=${RUST_LOG}
